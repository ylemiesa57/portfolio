# DEPRECATED — no longer used.
#
# The graph store moved from Neo4j to Boltless (github.com/ylemiesa57/boltless),
# a from-scratch, zero-dependency embedded graph engine served by the FastAPI
# app in /backend. There's no container to run for local dev anymore:
#
#   cd backend && pip install -r requirements.txt && uvicorn main:app --reload
#
# See KNOWLEDGE_GRAPH.md. This file is safe to delete.
